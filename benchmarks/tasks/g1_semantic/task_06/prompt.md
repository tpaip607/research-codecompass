You are working on the FastAPI RealWorld example app at /Users/tarak/engineer/repos/fastapi-realworld-example-app.

Task: Change the API prefix from "/api" to "/v1/api". This should update both the setting definition and anywhere the app mounts its routes.
