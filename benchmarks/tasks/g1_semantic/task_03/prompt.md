You are working on the FastAPI RealWorld example app at /Users/tarak/engineer/repos/fastapi-realworld-example-app.

Task: Change the JWT token prefix from "Token" to "Bearer" everywhere it is used in the codebase.

Update both the settings definition and any place that checks or constructs the prefix string.
