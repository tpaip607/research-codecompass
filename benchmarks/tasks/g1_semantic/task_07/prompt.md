You are working on the FastAPI RealWorld example app at /Users/tarak/engineer/repos/fastapi-realworld-example-app.

Task: Update the CORS allowed hosts from the default wildcard ["*"] to ["localhost:3000", "example.com"]. Update both the setting and where it is applied in the application factory.
