You are working on the FastAPI RealWorld example app at /Users/tarak/engineer/repos/fastapi-realworld-example-app.

Task: Create a new module app/api/errors/registry.py with a register_exception_handlers(app) function that registers all exception handlers. Move the exception handler registration logic out of app/main.py into this new function. Update main.py to call registry.register_exception_handlers(app).
