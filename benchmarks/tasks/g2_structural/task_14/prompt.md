You are working on the FastAPI RealWorld example app at /Users/tarak/engineer/repos/fastapi-realworld-example-app.

Task: Create a new module app/core/validators.py with a validate_database_url() function that checks the DATABASE_URL is a valid PostgreSQL URL. Call this validator from get_app_settings() in app/core/config.py after the settings object is created.
